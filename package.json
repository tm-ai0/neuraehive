{
  "name": "cinerae",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "CINERÆ — WebGPU particle instrument driven by webcam optical flow and microphone. NeuraeHive experiment evaluating vgpu.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "vgpu": "latest"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.60",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
